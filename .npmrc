# supply-chain cooldown: don't *resolve* deps published less than a day ago.
# A no-op for `npm ci` (lockfile install); protects `npm install`/`npm update`.
# Requires npm 11.x; older npm silently ignores it.
min-release-age=1
